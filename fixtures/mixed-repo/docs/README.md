# M3 repository guide

## Pipeline

The [transformation module](../pipeline/transform.py) reads the
[source ledger](../data/source.yaml) with [pipeline settings](../config/pipeline.json).
The [generated report](report.md) records the output and the
[decision](decisions/ADR-0001.md) explains the boundary.

The external [operations guide](https://example.com/operations) is shared with the report.
