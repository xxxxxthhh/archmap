# Keep transformations deterministic

The [Python transformation](../../pipeline/transform.py) may read the
[pipeline settings](../../config/pipeline.json), but dynamic output paths remain uncertain.
